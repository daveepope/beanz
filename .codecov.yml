coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

flag_management:
  default_rules:
    - flags:
        - linux
      paths:
        - "src/**"

ignore:
  - "target/**"
  - "tests/**"
  - "sync_agent_rules.py"
